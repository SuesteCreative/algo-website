{
  "name": "algo-website",
  "version": "2.0.0",
  "description": "Algo Atelier — Static site generated with Eleventy",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "preview": "browser-sync start --server _site --files '_site/**/*'"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "browser-sync": "^3.0.0",
    "markdown-it": "^14.1.0"
  }
}