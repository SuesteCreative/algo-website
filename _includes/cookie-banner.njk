{#
  cookie-banner.njk
  ────────────────────────────────────────────────────────────────
  Banner GDPR de consentimento de cookies.
  Bilíngue: deteta lang via variável `lang` (definida em cada template).
  Escondido por defeito — o JS (algo-tracking.js) torna-o visível
  apenas se não houver consentimento guardado.

  Incluir ANTES de </body> em todos os layouts.
  ────────────────────────────────────────────────────────────────
#}

<div
    id="cookie-banner"
    role="dialog"
    aria-modal="true"
    aria-labelledby="cookie-banner-title"
    aria-hidden="true"
    style="display:none;"
>
    <div class="cookie-banner__inner">

        {% if lang == 'en' %}
        {# ── ENGLISH ─────────────────────────────────────────── #}
        <div class="cookie-banner__text">
            <h2 id="cookie-banner-title" class="cookie-banner__title">Cookie Preferences</h2>
            <p class="cookie-banner__body">
                We use cookies to analyze traffic and optimize your experience. 
                Tracking is only activated with your explicit permission.
                <br><br>
                <a href="/en/legal/cookies/" class="cookie-banner__link">View detailed Cookie Policy</a>
            </p>
        </div>
        <div class="cookie-banner__actions">
            <button
                id="cookie-accept"
                class="cookie-banner__btn cookie-banner__btn--accept"
                onclick="acceptCookies()"
            >Accept All</button>
            <button
                id="cookie-reject"
                class="cookie-banner__btn cookie-banner__btn--reject"
                onclick="rejectCookies()"
            >Essential Only</button>
        </div>

        {% else %}
        {# ── PORTUGUÊS (default) ──────────────────────────────── #}
        <div class="cookie-banner__text">
            <h2 id="cookie-banner-title" class="cookie-banner__title">Preferências de Cookies</h2>
            <p class="cookie-banner__body">
                Utilizamos cookies para analisar o tráfego e otimizar a sua experiência. 
                O rastreio só é ativado com a sua permissão explícita.
                <br><br>
                <a href="/legal/cookies/" class="cookie-banner__link">Consultar Política de Cookies</a>
            </p>
        </div>
        <div class="cookie-banner__actions">
            <button
                id="cookie-accept"
                class="cookie-banner__btn cookie-banner__btn--accept"
                onclick="acceptCookies()"
            >Aceitar Todos</button>
            <button
                id="cookie-reject"
                class="cookie-banner__btn cookie-banner__btn--reject"
                onclick="rejectCookies()"
            >Apenas Essenciais</button>
        </div>
        {% endif %}

    </div>
</div>
