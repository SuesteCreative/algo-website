---
permalink: /servicos/
---
{% set pageTitle = "Serviços | Algo Atelier" %}
{% set metaDesc = "Descubra os serviços da Algo Atelier: licenciamento camarário, reabilitação urbana e projetos de arquitetura de luxo no Algarve e Grande Lisboa." %}
{% set canonical = site.siteUrl + page.url %}
{% set hreflangPt = site.siteUrl + ptUrl %}
{% set hreflangEn = site.siteUrl + enUrl %}
{% set activeSection = "servicos" %}
{% set ptUrl = "/servicos/" %}
{% set enUrl = "/en/services/" %}
<!DOCTYPE html>
<html lang="pt">
<head>{% include "head.njk" %}</head>
<body>
{% include "header-pt.njk" %}
<main>
    <section class="container" style="padding-top: var(--spacing-xl); padding-bottom: var(--spacing-lg);">
        <div style="max-width: 800px;">
            <span class="caps fade-up">Áreas de Atuação</span>
            <h1 class="page-title fade-up">{{ site.services.header_titles.title_pt }}</h1>
        </div>

        <div class="services-grid" style="display: grid; grid-template-columns: repeat(auto-fit, minmax(350px, 1fr)); gap: 5rem 4rem; margin-top: var(--spacing-lg);">
            {% for item in site.services.items %}
            <div class="service-card fade-up">
                {% if item.image %}
                <div class="service-image" style="width: 100%; height: 280px; overflow: hidden; margin-bottom: 2rem; border: 1px solid rgba(0,0,0,0.05);">
                    <img src="{{ item.image }}" alt="{{ item.title_pt }}" style="width: 100%; height: 100%; object-fit: cover; transition: transform 0.6s ease;">
                </div>
                {% endif %}
                <h3 class="caps" style="font-size: 0.85rem; margin-bottom: 1.5rem; letter-spacing: 0.15em;">{{ item.title_pt }}</h3>
                <p style="opacity: 0.7; line-height: 1.8; font-size: 0.95rem;">{{ item.text_pt }}</p>
            </div>
            {% endfor %}
        </div>
    </section>

    {% include "cta-section.njk" %}
</main>

<style>
.service-card:hover .service-image img {
    transform: scale(1.05);
}
@media (max-width: 768px) {
    .services-grid {
        grid-template-columns: 1fr !important;
        gap: 4rem !important;
    }
}
</style>
{% include "footer-pt.njk" %}
<script src="/assets/js/script.js"></script>
    {% include "tracking-init.njk" %}
</body>
</html>
