---
permalink: /en/services/
---
{% set pageTitle = "Services | Algo Atelier" %}
{% set metaDesc = "Architecture, interior design, and project management services at Algo Atelier." %}
{% set canonical = "https://algoatelier.netlify.app/en/services/" %}
{% set activeSection = "services" %}
{% set ptUrl = "/servicos/" %}
{% set enUrl = "/en/services/" %}
<!DOCTYPE html>
<html lang="en">
<head>{% include "head.njk" %}</head>
<body>
{% include "header-en.njk" %}
<main>
    <section class="container" style="padding-top: var(--spacing-xl); padding-bottom: var(--spacing-lg);">
        <div style="max-width: 800px;">
            <span class="caps fade-up active">Expertise</span>
            <h1 class="page-title fade-up active">{{ site.services.header_titles.title_en }}</h1>
        </div>

        <div class="services-grid" style="display: grid; grid-template-columns: repeat(auto-fit, minmax(350px, 1fr)); gap: 5rem 4rem; margin-top: var(--spacing-lg);">
            {% for item in site.services.items %}
            <div class="service-card fade-up active">
                {% if item.image %}
                <div class="service-image" style="width: 100%; height: 280px; overflow: hidden; margin-bottom: 2rem; border: 1px solid rgba(0,0,0,0.05);">
                    <img src="{{ item.image }}" alt="{{ item.title_en }}" style="width: 100%; height: 100%; object-fit: cover; transition: transform 0.6s ease;">
                </div>
                {% endif %}
                <h3 class="caps" style="font-size: 0.85rem; margin-bottom: 1.5rem; letter-spacing: 0.15em;">{{ item.title_en }}</h3>
                <p style="opacity: 0.7; line-height: 1.8; font-size: 0.95rem;">{{ item.text_en }}</p>
            </div>
            {% endfor %}
        </div>
    </section>

    {% set lang = 'en' %}
    {% include "cta-section.njk" %}
</main>

<style>
.service-card:hover .service-image img {
    transform: scale(1.05);
}
@media (max-width: 768px) {
    .services-grid {
        grid-template-columns: 1fr !important;
        gap: 4rem !important;
    }
}
</style>
{% include "footer-en.njk" %}
<script src="/assets/js/script.js"></script>
    {% include "tracking-init.njk" %}
</body>
</html>
